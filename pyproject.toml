[project]
name = "nvcenter"
version = "0.1.0"
description = "NvCenter: project scaffold with tests, ruff, CI, and fuzzing"
requires-python = ">=3.9"
dependencies = [
    "polars>=0.20",
    "typer>=0.12.0",
    "matplotlib>=3.7",
    "diskcache>=5.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.1",
    "hypothesis>=6.0",
    "ruff>=0.6.0",
    "pre-commit>=3.6",
]

[project.scripts]
nvcenter = "nvcenter.__main__:main"

[tool.ruff]
line-length = 100
src = ["src", "tests"]
exclude = [
    ".git",
    ".venv",
    ".idea",
    ".mypy_cache",
    "build",
    "dist",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # Pyflakes
    "W",   # pycodestyle warnings
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C90", # mccabe
    "COM", # flake8-commas
    "DTZ", # flake8-datetimez
    "SIM", # flake8-simplify
    "PT",  # flake8-pytest-style
    "RUF", # Ruff rules
]
ignore = [
]
fix = true

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --cov=nvcenter --cov-report=xml --cov-report=term-missing --cov-report=html"
testpaths = ["tests"]
pythonpath = ["src"]
filterwarnings = [
    "error::DeprecationWarning",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
